// Quartz v5 loads a plugin's components from its "./components" export subpath.
// Export names must match the `quartz.components` keys in package.json.
export { Footer, default } from "./Footer"
export type { FooterLink, FooterOptions } from "./Footer"
